Declare cada casilla con el document.getElementById, Tambien los botones y espacios donde necesito mostrar, hice una lista con cada casilla, inicialice las victorias de X y O en local storage y los inserte en los espacios del html.Cree una function de marcarCasillas con un forEach para recorrer la lista elemento por elemento con la accion del click y despues una condicion para no poder x cuando hay un ganador, lo siguiente es cambiar el style de la casilla para que la X salga de color azul, despues verifica que hay un ganador con la function Elegir ganador que funciona de la siguiente manera hay una lista de lista de las posibles posiciones ganadoras, despues de eso va un for of que itera la lista posicionesGanadoras dentro del ciclo hay una variable que sirve para comparar las posiciones, despues un if que la condicion donde compara la lista principal en la posicion uno que no haya un espacio vaciom que las demas posiciones sean igual a la primera, despues otro if donde la condicion es si lista en la posicion 1 es exactamente igual X entonces entra y suma una victoria a la X, pone victorias X en el locals storage y en jugador introduce el contenido de las victorias de X y por ultimo crea un h2 con el texto de felicidades has ganado y lo introduce en anuncioGanador. Es igual en el victoria de O y el empate funciona de la siguiente manera la condicion es para ver si se hicieron todos los movimientos posibles y si no hay ganador entonces muestra empate. Despues qei sale de la funcion elegirGanador hay un if que sirve para que la maquina no ponga un O si gana el jugador y dentro del if hay un setTimeout para dar un retraso a la funcion marcar circulo, ahora voy a explicar como funciona esta primero cree una constante para filtrar las casillas vacias y después otra constante para que me un numero aleatoria de las posiciones vacias, después que en la lista casillasVacias en las posiciones aleatorias inserte un O y después ejecute la funcion elegir ganador para ver si el circulo gano. La funcion reinicio recorre las lista entera y limpia cada elemento de la lista y también el h2 que dice quien gano y inicializa la variable de cantidad de movimientos. La funcion de btnReinicioMarcador limpia el loca storage, el marcador y recarga la pagina.